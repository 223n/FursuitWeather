{
  "name": "fursuit-weather",
  "version": "1.15.1",
  "private": true,
  "description": "着ぐるみ天気予報 - 気象データから着ぐるみ活動の適否を予報するサービス",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "build": "npm run minify && node scripts/inline-css.mjs && node scripts/optimize-sprite.mjs",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . && npm run typecheck",
    "minify": "esbuild public/app.js --minify --charset=utf8 --allow-overwrite --outfile=public/app.js && esbuild public/prefs.js --minify --charset=utf8 --allow-overwrite --outfile=public/prefs.js && esbuild public/wbgt-tool.js --minify --charset=utf8 --allow-overwrite --outfile=public/wbgt-tool.js && esbuild public/display.js --minify --charset=utf8 --allow-overwrite --outfile=public/display.js && esbuild public/sw.js --minify --charset=utf8 --allow-overwrite --outfile=public/sw.js && esbuild public/style.css --minify --charset=utf8 --allow-overwrite --outfile=public/style.css && esbuild public/display.css --minify --charset=utf8 --allow-overwrite --outfile=public/display.css"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260827.1",
    "@playwright/test": "^1.62.1",
    "@vitest/coverage-v8": "^4.1.11",
    "axe-core": "^4.13.0",
    "esbuild": "0.28.2",
    "eslint": "^10.9.1",
    "svgo": "^4.1.0",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.68.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.127.0"
  }
}
